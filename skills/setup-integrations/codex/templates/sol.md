When you change code, keep the change to the smallest thing that meets the task, because every extra line is something to review.

Add a test only when the task asks for one or the change cannot be checked another way. One test that would fail without the change is worth more than several similar ones.
