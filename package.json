{
  "name": "dotclaude",
  "version": "0.1.0",
  "private": true,
  "description": "Claude Code plugin: mechanical guardrails and settings profile.",
  "type": "module",
  "scripts": {
    "test": "bun test tests/",
    "validate": "claude plugin validate --strict . && claude plugin validate --strict .claude-plugin/plugin.json && claude plugin validate --strict skills && claude plugin validate --strict agents",
    "lint": "biome lint hooks tests skills"
  },
  "license": "MIT",
  "engines": {
    "bun": ">=1.4.2"
  }
}
