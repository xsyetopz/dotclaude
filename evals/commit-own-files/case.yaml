schema_version: "1.1"
name: commit-own-files
description: Committing a fix should stage only the files Claude changed and leave the user's uncommitted edit alone.
tags: [output-style]
context:
  scaffold_script: fixture.sh
