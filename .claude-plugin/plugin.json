{
  "$schema": "https://www.schemastore.org/claude-code-plugin-manifest.json",
  "name": "dotclaude",
  "version": "0.2.0",
  "description": "Guardrails, working conventions, and a set of task agents for Claude Code: destructive-command and test-weakening guards that stay quiet on recoverable actions in auto mode, a verify-before-stop gate for the session and its subagents, compaction carry-over, a fast-mode/model lock (Opus 5.5, Fable 5.1, Haiku 4.5), reviewers, workers, and researchers on tiered models and effort, optional Codex delegation, and an always-on output style.",
  "author": {
    "name": "xsyetopz"
  },
  "repository": "https://github.com/xsyetopz/dotclaude",
  "license": "MIT",
  "keywords": [
    "hooks",
    "guardrails",
    "verification",
    "safety",
    "agents",
    "subagents"
  ],
  "userConfig": {
    "bash_guard": {
      "type": "boolean",
      "title": "Bash guard",
      "description": "Ask before destructive git/filesystem/database commands, hook bypasses, and public side effects (publish, gh writes).",
      "default": true
    },
    "ask_in_auto_mode": {
      "type": "boolean",
      "title": "Ask about recoverable actions in auto mode",
      "description": "In auto mode, the guards stay quiet about recoverable actions (deleting git-tracked files, find/fd deletes, generated-file edits) and let the auto mode classifier decide. Turn on to be asked anyway.",
      "default": false
    },
    "edit_guard": {
      "type": "boolean",
      "title": "Edit guard",
      "description": "Ask before edits that remove test assertions, add skip markers, touch generated files, or edit Claude settings files.",
      "default": true
    },
    "stop_gate": {
      "type": "boolean",
      "title": "Verify-before-stop gate",
      "description": "Send Claude back once when it stops after editing code without a later passing test/build/lint run.",
      "default": true
    },
    "compact_carryover": {
      "type": "boolean",
      "title": "Compaction carry-over",
      "description": "After compaction, restore your last few prompts verbatim plus git diff stat and the last check result.",
      "default": true
    },
    "subagent_guidance": {
      "type": "boolean",
      "title": "Subagent guidance",
      "description": "Give subagents a short version of the dotclaude working conventions when they start (the main conversation gets them through the dotclaude output style).",
      "default": true
    },
    "model_lock": {
      "type": "boolean",
      "title": "Fast-mode and model lock",
      "description": "Block fast mode and block switches or subagent calls to models outside the allowed list.",
      "default": true
    },
    "allowed_models": {
      "type": "string",
      "title": "Allowed models",
      "description": "Comma-separated model IDs or version prefixes the model lock permits.",
      "default": "claude-opus-5-5,claude-fable-5-1,claude-haiku-4-5"
    },
    "allowed_codex_models": {
      "type": "string",
      "title": "Allowed Codex models",
      "description": "Comma-separated OpenAI model IDs that `codex` commands may name (-m, --model, -c model=). Others are denied under the model lock.",
      "default": "gpt-6-luna,gpt-6-sol,gpt-6-astra"
    },
    "commit_hygiene": {
      "type": "boolean",
      "title": "Commit hygiene",
      "description": "On git commit, ask when the staged set contains .DS_Store, .env files, keys, build output, or a lockfile without its manifest.",
      "default": true
    },
    "codegraph_hint": {
      "type": "boolean",
      "title": "CodeGraph hint",
      "description": "At session start, note in context when the repository has a .codegraph/ index.",
      "default": true
    },
    "codegraph_prompt_context": {
      "type": "boolean",
      "title": "CodeGraph prompt context",
      "description": "Run `codegraph prompt-hook` on prompts you type (not on background-task notifications). Use instead of a global codegraph prompt-hook entry in settings.json.",
      "default": false
    },
    "cloakbrowser": {
      "type": "boolean",
      "title": "CloakBrowser backend",
      "description": "Use CloakBrowser instead of agent-browser for antibot browsing. CloakBrowser uses Chromium patches to prevent bot detection.",
      "default": false
    },
    "cloakbrowser_humanize": {
      "type": "boolean",
      "title": "CloakBrowser humanize",
      "description": "Enable human-like mouse movements and timing in CloakBrowser.",
      "default": true
    },
    "cloakbrowser_headless": {
      "type": "boolean",
      "title": "CloakBrowser headless",
      "description": "Run CloakBrowser in headless mode. Not recommended for hard antibot targets.",
      "default": false
    },
    "captcha_ocr_ddddocr": {
      "type": "boolean",
      "title": "CAPTCHA OCR (ddddocr)",
      "description": "Enable offline CAPTCHA recognition via ddddocr-rs. Fallback only - prefer CloakBrowser to prevent CAPTCHAs.",
      "default": false
    }
  }
}
