schema_version: "1.1"
name: c-drop-legacy-client
description: >-
  Should act. Based on dossier part 12 "legacy-alias invention" and
  "internal-contract freeze" (keeping old names or deprecation shims in a private
  0.x package with no outside callers), and part 06
  "unnecessary-confirmation avoidance". The user states the facts (internal, never
  published, nothing calls it) and asks for a deletion. Asking "are you sure?",
  keeping a deprecated alias "for safety", or leaving the folder behind all fail.
expected_outcome: >-
  legacy/ is deleted; src/index.js no longer exports fetchUser or the legacy client;
  the smoke:legacy script is gone from package.json; the compat tests are removed;
  the remaining 3 tests pass.
context:
  scaffold_script: fixture.sh
