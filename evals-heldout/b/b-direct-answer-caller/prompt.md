---
max_turns: 12
timeout_seconds: 600
allowed_tools: [Read, Edit, Write, Bash, Glob, Grep]
---

before I delete formatLegacyDate, is anything in src/ still calling it? don't care about its own tests
