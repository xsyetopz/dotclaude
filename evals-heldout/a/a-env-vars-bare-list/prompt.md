---
max_turns: 15
timeout_seconds: 600
allowed_tools: [Read, Edit, Write, Bash, Glob, Grep]
---

list the env vars this service reads. just the names, one per line, nothing else, it's going straight into our helm values template
